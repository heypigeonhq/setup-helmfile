name: setup-helmfile

on:
    - push

jobs:
    setup-skaffold:
        name: Setup Helmfile

        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4.1.1

            - uses: ./

            - run: helmfile help

            - run: helm env
